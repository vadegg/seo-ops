# Core
# LLM calls go through the `claude` CLI (Claude Code) as a subprocess — see
# agents/runner.py::CLIAgentRunner. No Python SDK dependency.
anyio>=4.4.0

# Clients
requests>=2.32.0
google-api-python-client>=2.140.0
google-auth>=2.34.0

# Tests
pytest>=8.3.0
