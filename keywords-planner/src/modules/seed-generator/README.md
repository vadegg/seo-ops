# seed-generator

Reserved for `Module 0`.

This module is intentionally a placeholder in `v1`.

Future responsibility:

- derive initial seed keywords from the site profile
- output normalized seeds for the rest of the pipeline

Current `v1` behavior:

- seeds are provided manually in `input/seeds.yaml`
